server:
  port: 8083
spring:
  profiles:
    active: ${env-flag}
  application:
    name: ape-cloud-sku
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:7001/eureka,http://localhost:7002/eureka