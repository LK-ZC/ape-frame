server:
  port: 8081
spring:
  profiles:
    active: ${env-flag}
  application:
    name: ape-cloud-home
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:7001/eureka,http://localhost:7002/eureka